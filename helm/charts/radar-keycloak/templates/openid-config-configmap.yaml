apiVersion: v1
kind: ConfigMap
metadata:
  name: radar-keycloak-openid-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{ include "common.labels.standard" . | indent 4 | trim }}
data:
  openid-configuration.json: |-
{{ tpl ((.Files.Get "files/openid-configuration.json")) . | indent 4 }}
